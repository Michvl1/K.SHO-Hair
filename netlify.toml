# netlify.toml — Netlify configuration for K.SHO Hair

[build]
  # Where your Netlify Functions live
  functions = "netlify/functions"
  # Serve the site from the repo root (index.html, images, etc.)
  publish = "."
